services:
  backend:
    build:
      context: ./backend
    ports:
      - "3000:3000"
    environment:
      GOOGLE_MAPS_API_KEY: ${GOOGLE_MAPS_API_KEY}
      PORT: ${PORT:-3000}
    restart: unless-stopped
